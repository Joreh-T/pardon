---@mod pardon.card Word-card lookup + rendering (placeholder)
--
-- Task 23 fills this in (lookup contract → floatwin card). Kept as an empty
-- module so `require('pardon')` and plugin/pardon.lua load in the skeleton.

local M = {}
return M
