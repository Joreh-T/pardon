{
  "name": "pardon-gui-ui",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "check": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "x-note": "test script uses --passWithNoTests until Task 8 lands real frontend tests",
  "dependencies": {
    "@tauri-apps/api": "^2"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^7.0.0",
    "vitest": "^3.0.0"
  }
}
